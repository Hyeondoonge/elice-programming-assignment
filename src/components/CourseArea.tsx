import React from 'react';
import styled from 'styled-components';

const StyledCourseArea = styled.div`
  width: 100%;
`;

export default function CourseArea() {
  return <StyledCourseArea>강좌목록영역</StyledCourseArea>;
}
