import React from 'react';
import styled from 'styled-components';

const StyledFilterArea = styled.div`
  width: 100%;
`;

export default function FilterArea() {
  return <StyledFilterArea>필터영역</StyledFilterArea>;
}
